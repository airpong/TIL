import React from "react";
import { View, Text } from "react-native";

export default ({ Navigation, route }) => {
  console.log(route);
  return (
    <View>
      <Text>Detail</Text>
    </View>
  );
};
